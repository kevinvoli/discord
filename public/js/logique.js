const settings= document.getElementById('#settings')
const addcontact=  document.getElementById('#settings')

settings.addEventListener((e)=>{
    e.preventDefault()

})

addcontact.addEventListener((e)=>{
    e.preventDefault()

})